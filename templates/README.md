# Templates

Reusable skeletons for environment-specific files that should be
copied-and-edited per project, not committed with values.

| File | Purpose |
| --- | --- |
| `.env.example` | Example `.env` for projects. NEVER rename to `.env` in this repo. |

See `docs/secrets-policy.md` for what must never be committed.