# Shared manifests

Manifests that apply across platforms (for example, a single cloud
resource list, or a vendor-neutral package definition) would live here.

Currently empty by design. Populate when a real cross-platform manifest
appears.