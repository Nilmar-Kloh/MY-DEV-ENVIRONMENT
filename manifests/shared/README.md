# Manifests — shared

Cross-platform manifests would live here. None exist at present.

The Brewfile is macOS-only and stays at the repo root by Homebrew
convention. The winget manifest is Windows-only and lives at
`platforms/windows/packages.txt`.